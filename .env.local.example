# ============================================================
# 데일리 스크럼 환경 변수 예시
# 이 파일을 복사하여 .env.local 을 생성하고 실제 값을 입력하세요
# cp .env.local.example .env.local
# ============================================================

# ---- Public 변수 (클라이언트에서도 접근 가능) ----
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=데일리 스크럼

# ---- Private 변수 (서버 전용) ----

# OpenAI API 키 (AI 스크럼 자동 생성에 사용, gpt-4o-mini 모델)
# https://platform.openai.com/api-keys 에서 발급
OPENAI_API_KEY=

# Notion Integration Token (서버 전용)
# https://www.notion.so/my-integrations 에서 Internal Integration 생성 후 발급
NOTION_API_KEY=secret_...

# 워크로그를 업로드할 Notion 데이터베이스 ID
# 데이터베이스 URL: notion.so/.../{DATABASE_ID}?v=...
# 필수 속성: 이름(Title), 날짜(Date), 태그(Select), 상태(Status), 순서(Number)
NOTION_DATABASE_ID=

# ---- Supabase 설정 (Phase 2 필수) ----
# Supabase 대시보드 > Project Settings > API 에서 확인
# https://supabase.com/dashboard

# 프로젝트 URL (클라이언트에서 접근 가능)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# anon 공개 키 (클라이언트에서 접근 가능, RLS로 보호됨)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# service_role 키 (서버 전용 — 절대 NEXT_PUBLIC_ 붙이지 않음)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key